# LTN for Semantic PASCAL-Part

## Data
Generate the hdf5 data by following the instructions in the [semantic-pascal-part repository](https://github.com/sbadredd/semantic-pascal-part) and place them in `data/trainval` and `data/test`.

## Run
Choose some setting in `config.yml` and the experiment using `python train.py`